<Page
    x:Class="ImageCanvas.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ImageCanvas"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="10" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Button x:Name="BeginButton"  Grid.Row="0" Click="BeginButton_OnClick">BEGIN</Button>
            <TextBox x:Name="JsonTextBox" Grid.Row="1"  PlaceholderText="Paste JSON Text HERE" AcceptsReturn="True"  />
        </Grid>
        <ScrollViewer ZoomMode="Enabled" Grid.Row="1" BorderBrush="Gray" BorderThickness="1" >
            <Grid>
                <Canvas x:Name="MyCanvas">

                </Canvas>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
