<UserControl
    x:Class="Universal.Edge.UserControls.DisplayItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Universal.Edge.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0">
            <TextBlock Text="{Binding Text}"  />
            <TextBlock Text="{Binding Index}"  />
        </StackPanel>
        <Grid Grid.Row="0" Grid.Column="1">
            <Button  Content="Add" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="Add_OnClick"/>
        </Grid>
        <Grid Grid.Row="0" Grid.Column="2">
            <Button  Content="Close" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="Close_OnClick"/>
        </Grid>
    </Grid>
</UserControl>
