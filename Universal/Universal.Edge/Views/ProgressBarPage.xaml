<Page
    x:Class="Universal.Edge.Views.ProgressBarPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Universal.Edge.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid>
            <ProgressBar x:Name="AnimatedProgressBar" />
        </Grid>
        <Grid>
            <Slider x:Name="SpeedSlider"  Maximum="100" Value="40" />
        </Grid>
        <Grid Grid.Row="2">
            <StackPanel>
                <TextBox x:Name="CountTextBox" PlaceholderText="Count in numbers only" Text="100" />
                <TextBox x:Name="AnimationSpeed" PlaceholderText="Speed in seconds only" Text="0" />
                <Button x:Name="StartButton" Content="Start" Click="StartButton_OnClick" />
                <Button x:Name="StepButton" Content="Step" Click="StepButton_OnClick" />
                <Button x:Name="CompleteButton" Content="Complete" Click="CompleteButton_OnClick" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
